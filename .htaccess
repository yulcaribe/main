Options -Indexes

RewriteEngine On
# Keep the public Briefing URL stable for web and Android while enriching the
# deterministic briefing response with local FAA NMS NOTAM relevance.
RewriteRule ^api/v1/briefing\.php$ api/v1/briefing-notam.php [L,QSA]
